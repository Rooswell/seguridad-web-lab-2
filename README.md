# Seguridad Web Lab 2 – SSRF

Proyecto Laravel que demuestra una vulnerabilidad SSRF y su mitigación.

## Ejecutar

composer install
php artisan serve

## Endpoints

POST /api/ssrf/vulnerable-fetch
POST /api/ssrf/safe-fetch
